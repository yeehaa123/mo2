%li
	= gravatar_for user, size: 52
	= link_to user.user_name, user
	- if can? :destroy, @user
		#{"/"}
		= link_to "delete", user, method: :delete, data: { confirm: "You sure" } unless current_user?(user)